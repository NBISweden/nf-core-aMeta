Changes in component 'nf-core/maltextract'
'modules/nf-core/maltextract/environment.yml' is unchanged
Changes in 'maltextract/meta.yml':
--- modules/nf-core/maltextract/meta.yml
+++ modules/nf-core/maltextract/meta.yml
@@ -54,6 +54,19 @@
           type: directory
           description: Directory containing MaltExtract text results files
           pattern: "results/"
+  ref_id:
+    - - meta:
+          type: map
+          description: |
+            Groovy Map containing sample information
+            e.g. [ id:'test', single_end:false ]
+      - ref_id.txt:
+          type: file
+          description: |
+            Reference sequence ID extracted from the readDist additionalNodeEntries
+            report, present only when a valid ref ID (distinct from the taxid) was found.
+          pattern: "ref_id.txt"
+          ontologies: []
   versions:
     - versions.yml:
         type: file

Changes in 'maltextract/main.nf':
--- modules/nf-core/maltextract/main.nf
+++ modules/nf-core/maltextract/main.nf
@@ -14,6 +14,7 @@
 
     output:
     tuple val(meta), path("results")      , emit: results
+    tuple val(meta), path("ref_id.txt")   , emit: ref_id, optional: true
     path "versions.yml"                   , emit: versions
 
     when:
@@ -31,6 +32,15 @@
         -o results/ \\
         $args
 
+    # Equivalent to get_ref_id() in aMeta/workflow/rules/common.smk
+    REF_ID_FILE=\$( find -L results -wholename "*/default/readDist/*.rma6_additionalNodeEntries.txt" )
+    if [ -f "\$REF_ID_FILE" ]; then
+        REF_ID=\$( awk -F';_' 'NR==2 { print \$2 }' "\$REF_ID_FILE" )
+        if [ -n "\$REF_ID" ] && [ "\$REF_ID" != "${meta.taxid}" ]; then
+            echo "\$REF_ID" > ref_id.txt
+        fi
+    fi
+
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
         maltextract: \$(MaltExtract --help | head -n 2 | tail -n 1 | sed 's/MaltExtract version//')

'modules/nf-core/maltextract/tests/main.nf.test.snap' is unchanged
'modules/nf-core/maltextract/tests/main.nf.test' is unchanged
************************************************************
